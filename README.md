# Pinball 초기 데모 (Pygame + Pymunk)

간단한 Pygame 창을 띄우고 Pymunk 물리엔진으로 공 하나가 바닥으로 떨어지는 예제입니다.

설치 (권장: 가상환경 사용):

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

실행:

```bash
python main.py
```

ESC 또는 창 닫기로 종료합니다.
